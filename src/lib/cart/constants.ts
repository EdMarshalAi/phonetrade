/** Upper bound for a single line-item quantity (no real stock data yet). */
export const MAX_QTY = 10;

/** Защита Server Action от огромной пользовательской корзины. */
export const MAX_ORDER_LINES = 50;
