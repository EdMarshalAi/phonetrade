/** Юридические категории писем (38-ФЗ / ФАС). Чистый модуль (клиент + сервер). */
export type LegalCategory = "transactional" | "service" | "marketing";

export const LEGAL_LABEL: Record<string, string> = {
  transactional: "Транзакционное",
  service: "Сервисное",
  marketing: "Маркетинговое",
};

export const LEGAL_HINT: Record<string, string> = {
  transactional: "Отправляется всегда при наличии e-mail. Согласие не нужно, отписки нет. В письме НЕ должно быть рекламы, промокодов и подборок других товаров.",
  service: "Отправляется всем, кроме полной отписки. Согласие на маркетинг не требуется, но ссылка отписки есть. Без промокодов и рекламных подборок.",
  marketing: "Только при согласии на маркетинг. Ссылка отписки обязательна. Здесь можно акции, промокоды, подборки.",
};

export const LEGAL_WARNING =
  "Меняя категорию, проверьте содержимое. Транзакционные и сервисные письма должны быть БЕЗ рекламы, промокодов и подборок чужих товаров — иначе риск штрафа ФАС до 800 000 ₽ за нарушение.";

/** Цвет-индикатор категории (hex, для inline-style — надёжно под кастомную палитру). */
export function legalColor(c: string): string {
  return c === "transactional" ? "#16a34a" : c === "service" ? "#d97706" : "#e30000";
}
