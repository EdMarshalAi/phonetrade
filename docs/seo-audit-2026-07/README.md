# SEO-аудит PhoneTrade — сводка (claude-seo, 02.07.2026)

Полный аудит `phonetrade31.ru` по набору **claude-seo** (25 скилов). Каждый скил — отдельный файл в этой папке. Метод: чтение методологии скила + проверка реального кода и живого сайта (curl/WebFetch). Цель — **топ по Белгороду в Яндексе (приоритет) + Google**.

**Общая оценка: техбаза сильная (~82/100).** HTTPS+HSTS, чистый SSR, богатая schema с `@id` (Product/Offer/Store/FAQPage/Breadcrumb/ItemList/WebSite), robots под Яндекс (Clean-param+Host), llms.txt, LocalBusiness с полным NAP и `areaServed` по области, гео-страницы городов. Проблемы — не в фундаменте, а в точечных дырах и в последствиях массового залива 12.06.

---

## 🗓 СТАТУС ИСПОЛНЕНИЯ — читать первым (обновлено 03.07.2026)

> **Дополнение 13.07.2026:** выполнена повторная сверка документации, production-данных и кода. Подтверждённые технические/commerce-регрессии исправлены без изменений в Яндекс.Вебмастере и без массовой перезаписи контента. Актуальный отчёт и проверки: [`fix-report-2026-07-13.md`](./fix-report-2026-07-13.md). Он имеет приоритет над устаревшими рекомендациями ниже про `Host`, фиктивный `oldprice`, `priceValidUntil`, `shippingDetails` и `hasMerchantReturnPolicy`.

> **Будущему агенту:** это hand-off. Сначала пойми, что уже сделано, ПРОВЕРЬ состояние (см. «Когда и как продолжить»), и только потом бери задачи из списка «Осталось». НЕ повторяй уже сделанное. НЕ лей массово контент/мету — залив 12.06 уже наказал за это волатильностью.

### ✅ Сделано 03.07.2026 (коммиты в main)
**Фаза 0 — стабилизация:**
- Каннибализация блога: 4 поста-дубля → 301/308 на канонические (`next.config.ts` redirects) + старые архивированы в БД (`status='archived'`). Пары: `kakoy-iphone-vybrat-2026`→`…-sravnenie`, `bu-ili-novyj-iphone`→`new-or-used-…`, `macbook-air-ili-pro-belgorod`→`…-macbook-pro-2026`, `kakoy-ipad-vybrat-belgorod`→`kak-vybrat-ipad-2026`.
- Drift-baseline снят → `docs/seo-baseline-2026-07-03.md` (эталон для детекта регрессий).

**Фаза 1 — тех. быстрые победы:**
- **IndexNow** внедрён: `public/f7eec1c6f979d67e7220e5c92a5dd759.txt` + `src/lib/seo/indexnow.ts` (`pingIndexNow`) врезан в `adminMutation` → шлёт в Яндекс+Bing напрямую (агрегатор api.indexnow.org капризен). Работает автоматом при правках через админку.
- **YML-фид** починен: было 9 товаров (только accessories) → **226** (`shop_settings.yml_feed_prefs.categories=null`, `includeUsed=false`). Б/У в фид пока НЕ включены.
- **LCP**: `fetchPriority="high"` на `ProductGallery`.
- **Sitemap-гигиена** (`src/app/sitemap.ts` + `getSitemapProducts`): noindex-товары исключены, убран `lastModified` у статики/категорий (ISR давал ложную свежесть), дедуп URL.
- **og:image** главной + категорий (переопределение `openGraph` глушило наследование).

**Находки Вебмастера (03.07) отработаны:**
- **Clean-param** в `src/app/robots.txt`: добавлены `page&etext&url&butrub&tubl&tuble&tuble3` (краул-мусор из статистики обхода).
- **Капитализация** `products.title`/`meta_title` (прямой SQL в БД, ~44 товара): `IPad→iPad`, ` м3→ M3`, `128gb→128GB`, `Xr→XR`.
- **Дубли title 52→0**: Б/У `meta_title` уникализированы шаблоном «{title} — Б/У, АКБ {battery}%, Белгород» (+артикул при коллизиях), Dyson «с кейсом» дочищен. Всё прямым SQL в БД.

**Оптимизационный релиз (03.07):**
- H1 категорий с гео (`CatalogHero.tsx`), og:image категориям, домашний `<title>` с интентом (`src/app/layout.tsx`), Offer `shippingDetails`+`hasMerchantReturnPolicy` (`product/[id]/page.tsx`), FAQ на `/repair` (видимый блок в `RepairShell` + `FAQPage` из `src/lib/repair/faq.ts`), Apple Maps в `sameAs` (regex `maps.apple` + запись в `shop_settings.general.contacts`; 2ГИС там уже был).
- **IndexNow bulk-пинг**: все 426 URL sitemap отправлены на переобход (Яндекс 202, Bing 200).

**Состояние Яндекс.Вебмастера на 03.07:** диагностика чистая (санкций/фильтра нет, «ранжируются без понижения»), клики +26%. Вывод: просадка 12.06 — временная волатильность-переоценка, не санкция.

### ⏳ Осталось (делать ТОЛЬКО после паузы и проверки, партиями)
1. **Контент-кластеры** партиями по 3–5 статей (НЕ 15 разом): наушники Apple, смарт-часы Apple, ноутбук Apple/iMac/Mac mini. Хабы + спицы. Готовые брифы — в `seo-content-brief.md`, план кластеров — `seo-cluster.md`.
2. **Контент отзывов** на сайте (из реальных Яндекс.Бизнес/2ГИС, НЕ выдумывать) — `seo-local.md`. `aggregateRating` НЕ добавлять (решение владельца).
3. **FAQ-схема на категориях** — нужен осмысленный ВИДИМЫЙ FAQ-контент (как сделано на `/repair`) — `seo-geo.md`.
4. **Страницы-сравнения «X vs Y»** (iPhone 17 vs 17 Pro, AirPods 4 vs Pro) — `seo-competitor-pages.md`.
5. **E-E-A-T блога**: автор-человек, `dateModified`, блок «Похожие статьи» (посты-сироты по перелинковке) — `seo-content.md`.
6. **Программатик услуга×город** (`/remont-apple-{город}`) батчами по 5–10 с уникальным блоком — `seo-programmatic.md`.
7. **Прайс ремонта** на `/repair` (SXO-персона «ремонт») — `seo-sxo.md`.
8. **Citations**: Zoon/Flamp с консистентным NAP — `seo-backlinks.md`.

### ⛔ НЕ делать
- НЕ восстанавливать `opengraph-image.tsx` (удалён намеренно, коммит `9c600ad` — глушил per-page og товаров).
- НЕ подключать DataForSEO (всё покрыто бесплатно), НЕ добавлять hreflang (моногео), НЕ выдумывать отзывы/`aggregateRating`.
- НЕ объединять Б/У-товары-экземпляры в один без явного разрешения владельца.

### 📅 Когда и как продолжить
**Не раньше ~14.07.2026** (1–2 недели паузы — дать Яндексу/Bing переварить накопленное; IndexNow уже пнул переобход).

**Перед продолжением в новой сессии ОБЯЗАТЕЛЬНО:**
1. Попросить у пользователя свежие данные/скриншоты **Яндекс.Вебмастера** (позиции, выпавшие страницы, диагностика) и новую выгрузку позиций (сравнить с `../PhoneTrade_Yandex…xlsx` от 25.06).
2. Пересравнить `docs/seo-baseline-2026-07-03.md` с живым сайтом (curl тех же 7 URL) — нет ли регрессий title/canonical/schema.
3. **Если позиции стабильны/растут** → брать задачи «Осталось» партиями (контент-кластеры первыми, по 3–5). **Если просадка/выпадения** → сначала диагностика в Вебмастере, НЕ наслаивать новые правки.
4. Всегда: снять новый drift-baseline ДО начала следующей крупной партии.

Связанные заметки в памяти: [[yandex-rank-drop-2026-06-13]], [[webmasters-verified]], [[seo-audit-c1-c8-done]].

---

## 🔴 Критично (делать в первую очередь)

### Стабилизация после залива 12.06 (пока НЕ трогать массово)
- **Заморозить любые site-wide заливы** до отыгрыша Яндекса — каждый массовый ре-деплой сбрасывает таймер переоценки. Причина просадки = одномоментный дрейф `title/meta/description_html` у 317 товаров + категорий; baseline не снимался. → `seo-drift.md`, `seo-plan.md`.
- **Диагностика в Яндекс.Вебмастере**: переспам/Баден-Баден после залива, выпавшие страницы, 4xx/5xx за 12–13.06.
- **Завести `seo-drift baseline`** эталонных URL сейчас + сделать `drift compare` пред-деплойным гейтом (страховка от будущих регрессий). → `seo-drift.md`.

### Каннибализация блога (вероятная причина волатильности)
- **≥5 пар постов бьют в один запрос** (два «какой iPhone выбрать», два про MacBook, два про iPad, «Б/У или новый» ×2, несколько транзакционных «купить iPhone 17 Белгород»). Яндекс «мигает» между дублями → слить/301 в канонические URL. → `seo-content.md`, `seo-cluster.md`.

### Технические потери (быстрые, низкий риск)
- **IndexNow не внедрён** (`/indexnow` → 404). Поддерживают **Яндекс и Bing** → мгновенная переиндексация при смене цен/наличия. Точка врезки уже есть (`adminMutation` + `revalidatePath`). → `seo-technical.md`.
- **YML-фид отдаёт 9 товаров вместо ~300** (`/api/feed/yml`). `yml_feed_prefs.categories` режет выборку до аксессуаров/Dyson — iPhone/iPad/Mac/Watch/Samsung **не идут** в Яндекс.Товары и рекламу ВК. Чинится в админке за минуты. → `seo-ecommerce.md`.
- **LCP-картинка товара без `fetchpriority="high"`** — `priority` в коде есть, но в живом HTML теряется (вероятно `motion`/`AnimatePresence` в Next 16). Удар по LCP на 300+ карточках. → `seo-images.md`.
- **`opengraph-image.tsx` → 404** (динамический OG-баннер удалён, доки устарели). OG товара = голое фото без цены/бренда → слабый шеринг. Восстановить `next/og` (шрифт `_og/PTSans.ttf` на месте). → `seo-schema.md`, `seo-image-gen.md`.

### Локальный траст
- **Отзывов на сайте нет вообще** (0 `Review` в `src/`) — только badge-виджет Яндекс.Бизнеса. Trust — топ-фактор локалки. `aggregateRating` не делаем (решение владельца), но **контент отзывов** вывести стоит. → `seo-local.md`.
- **2ГИС и Apple Maps не в коде** — статически только Яндекс. 2ГИС для Белгорода — вторая карта, Apple Maps — под iPhone-аудиторию. `sameAs`-архитектура (`layout.tsx:54-58`) их уже поддержит. → `seo-maps.md`.

---

## 🟡 Важно (после стабилизации)

- **H1 категорий не локализован** (`/category/iphone` → H1 «iPhone» вместо «Купить iPhone в Белгороде») + **ломаная капитализация title** («IPad», «128gb», «м3») — бьёт по точным вхождениям (Яндекс чувствителен). → `seo-page.md`, `seo-technical.md`.
- **Sitemap-гигиена**: `/trade-in` дублируется дважды; noindex-товары (`is_indexable=false`) попадают в карту; `lastmod` = `now` (ложная свежесть). → `seo-sitemap.md`.
- **Домашний `<title>` без интента** — добавить «купить/цена/гарантия». → `seo-plan.md`, `seo-google.md`.
- **Offer без `shippingDetails` и `hasMerchantReturnPolicy`** → товары не проходят в Google Merchant free listings. → `seo-google.md`, `seo-ecommerce.md`.
- **Рассинхрон цены**: `meta_description` (47 000 ₽) vs Offer JSON-LD (51 000) — статик-мета устаревает при смене цены. → `seo-ecommerce.md`.
- **robots не управляет AI-краулерами** (нет директив GPTBot/YandexBot и т.д.) + **FAQ только на статических страницах** (нет на `/`, `/repair`, категориях — где локальные PAA). → `seo-geo.md`, `seo-local.md`.
- **Слабые/пустые кластеры** (наушники/часы/ноутбуки — по 1 посту; AirPods+Watch слиты в 1 URL) — источник «невидимости» из Вебмастера. 6 предложенных хабов + 2 готовых брифа. → `seo-cluster.md`, `seo-content-brief.md`.
- **E-E-A-T и структурные элементы**: нет автора-человека, `dateModified`=`datePublished`, нет «Похожих статей» (посты-сироты), нет FAQPage на категориях, нет страниц-сравнений «X vs Y». → `seo-content.md`, `seo-competitor-pages.md`.
- **На `/repair` нет прайса ремонта** — бьёт по SXO-персоне «ремонт». → `seo-sxo.md`.
- Проверить **дубль FAQPage** на карточке (из меморандума 13.06 — возможно уже устранён). → `seo-google.md`.

---

## 🟢 Nice-to-have / стратегия

- **Программатик услуга×город** (`/remont-apple-{город}`) — гео-страницы городов уже здоровый слой (~50% уникальности), масштабировать **батчами по 5–10**, наращивая уникальный блок (иначе thin). → `seo-programmatic.md`.
- **Отзывы на Яндекс.Картах/2ГИС** — самый дешёвый рычаг для локала и AI-видимости. → `seo-local.md`, `seo-maps.md`.
- **Ссылки — citations-first**: Яндекс.Бизнес + 2ГИС + Zoon/Flamp с консистентным NAP, без exact-match анкоров. Просадка 12.06 — не ссылочная. → `seo-backlinks.md`.
- **hreflang — задокументировать «не используем намеренно»** (моно-ru/моногео, состояние идеальное). → `seo-hreflang.md`.
- **DataForSEO — НЕ подключать**: всё покрыто бесплатно (Вордстат/Вебмастер/Topvisor). → `seo-dataforseo.md`.

---

## Рекомендованный порядок (фазами)

| Фаза | Что | Риск |
|---|---|---|
| **0. Стабилизация** | Заморозить массовые заливы · диагностика Вебмастера · seo-drift baseline · точечно near-duplicate мета не-iPhone · каннибализация блога (301) | низкий |
| **1. Тех. быстрые победы** | IndexNow · YML-фид (все товары) · fetchpriority LCP · восстановить OG · sitemap-гигиена · H1 категорий + нормализация title · интент в домашний title · Merchant-поля Offer | низкий |
| **2. Локал + контент** | Контент отзывов · 2ГИС/Apple Maps в sameAs · FAQ на /repair+категории+главную · прайс ремонта · наполнить кластеры · страницы-сравнения · E-E-A-T шаблон | средний |
| **3. Авторитет** | Citations (Яндекс.Бизнес/2ГИС/Zoon) · гео услуга×город батчами · переобход гэп-статей | низкий |

**Принцип:** Фаза 0 — только точечно, без батчей, пока Яндекс переваривает залив. Фазы 1–3 — по одной, с `drift baseline` до каждой.

---

## Файлы по скилам
Технический: `seo-technical` · `seo-sitemap` · `seo-page` · `seo-audit`
Локальный/AI: `seo-local` · `seo-maps` · `seo-geo` · `seo-sxo`
E-commerce/schema: `seo-ecommerce` · `seo-schema` · `seo-images` · `seo-image-gen`
Контент: `seo-content` · `seo-content-brief` · `seo-cluster` · `seo-competitor-pages`
Рост/мониторинг: `seo-drift` · `seo-programmatic` · `seo-hreflang` · `seo-backlinks`
Оркестрация/инструменты: `seo-plan` · `seo-flow` · `seo-google` · `seo-dataforseo` · `seo`
